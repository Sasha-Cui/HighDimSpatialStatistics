# Legacy Notebooks

All original notebooks are preserved under `notebooks/legacy/` for reference.
They are not the source of truth for the pipeline; use `src/HighDimSpatial/` and `scripts/pipeline/` instead.
